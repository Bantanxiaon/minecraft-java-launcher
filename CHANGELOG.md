# 版本说明

版本号固定三位：主版本.次版本.补丁版本。最右一位是小补丁/Bug 修复，中间一位是稍大补丁更新，最左一位是包含 UI 与新增内容的大型更新。每个版本在 `src/versionHighlights.ts` 维护“版本亮点”，软件首页自动展示。

## SH启动器 0.8.0 Beta - 2026-08-16

- 依赖解析可信化：不再从 modId 盲猜 Modrinth slug；内置已核对的 provider 别名映射（KotlinForForge、Bookshelf、Prism、Alex's Caves、Iron's Spells、TaCZ、ExpandAbility、fzzy_config、L2Library、Goety），Fabric/Quilt 的 provides 声明参与依赖满足判断。
- 内容来源溯源：Modrinth / CurseForge 安装写入 content_provenance（provider、projectId、fileId、来源 URL、SHA），建立可信 provenance。
- 标准离线 UUID：按 Java nameUUIDFromBytes 语义生成，保留旧 SHA-256 UUID 兼容字段；display_name 不再唯一，minecraft_uuid 唯一索引。
- 存储管理：磁盘占用总览、一键安全清理（预览 + fingerprint 二次校验）、已删除实例列表/恢复/永久删除。
- 数据库：新增 6 组表与真实 Migration（v6/v7/v8），开启 WAL 与 busy_timeout，升级前自动备份数据库并保留最近 5 份。
- 下载：新增单任务取消，任务间互不影响；数据目录无 D 盘时自动使用系统本地应用数据目录，仍优先沿用旧 D 盘数据。
- 一键免费联机 V1：e4mc 作为受管理内容精确安装（project identity + hash），启动后自动识别局域网端口与邀请地址，游戏退出自动清理房间状态。
- 路径安全：统一 Windows 文件名校验（保留设备名、结尾点/空格、禁用字符）。
- 工程化：Vitest 前端测试、CI 质量门禁（lint/test/build + cargo fmt/clippy/test）、版本同步脚本、BuildInfo 注入 commit/构建时间。

## SH启动器 0.7.0 Beta - 2026-08-16

- 整合包 = 独立实例（对齐 PCL）：导入或下载任何整合包都自动创建一套独立实例，自动读取游戏版本与加载器；通用 ZIP 可手动选择版本与加载器后同样生成独立实例。
- 自动匹配 Java：按游戏版本自动选择/安装对应 Java（8 / 17 / 21），启动前自动校核，不再手动配置。
- 整合包库：导入过的整合包集中记录，显示格式、游戏版本、加载器、大小、对应实例与所需 Java，支持一键再次导入、移除记录。
- 下载体验：切页面下载不中断；底部全局进度条点击打开实时下载详情（文件名、大小、进度、速度、剩余时间、重试次数）；下载期间其他操作不再被锁死。
- 搜索扩容与汉化：同时搜索 Modrinth 与 CurseForge（含公开代理），结果名称与简介自动翻译为中文（内置常用模组词典 + 在线翻译缓存），分类显示中文。
- 搜索不再卡住：结果即时返回，简介在后台逐条自动汉化（短超时 + 缓存），失败自动回退原文。
- CurseForge 链路加固：文件信息与下载增加可用镜像回退，实测真实 21.8MB 文件完整下载。
- UI 全面整改：新增“新界面”主题（默认），统一配色、卡片、按钮与布局，清理无功能控件；设置中可一键切回“经典界面”，功能完全一致，可随时回退。
- 底部全局读条改为“全部任务总进度”，点击弹窗可查看每个下载/安装目标的小进度、速度、剩余时间与实例安装进度。
- 侧栏新增“使用教程”：内置完整中文教程（首次使用、整合包、模组、联机、下载、设置、更新与日志）。
- 新增“开始游戏引导”：首次启动自动弹出 7 步引导（创建配置 → 装游戏 → 开始游戏 → 整合包 → 联机 → 更多功能），主页也有按钮可随时重新打开。
- 关键操作（搜索、整合包导入/下载）写入运行日志，遇到问题可直接查 D:\MinecraftLauncherData\logs\launcher.log。
- 修复崩溃级回归：此前把 kotlinforforge 当成“永远已安装”，导致缺它时启动前检查放行、进游戏才崩（如落幕曲的 fzzy_config / blue-archive-halo）。现在改为真实检测 mods 目录里的 kotlinforforge 文件，缺失时报告并在自动补齐时从 Modrinth 官方项目（Kotlin for Forge）下载安装；已补两条回归测试防止再犯。
- 弹窗统一支持右上角 × 关闭；底部全局读条只显示下载/处理状态，不再随页面消息变化；搜索汉化词典扩充到常用模组/整合包 200+ 条，并增加备用翻译源，翻译中会显示“汉化中…”提示。
- 真包测试通过：落幕曲 1.6.5 安装包（497MB）实测识别为 CurseForge / Forge / 1.20.1，266 个模组、5846 个覆盖文件，检查耗时约 0.43 秒。

## SH启动器 0.6.1 Beta - 2026-08-15

- 服务器延迟检测改为异步 TCP 连接，检测过程中界面保持流畅，不再卡顿。

## SH启动器 0.6.0 Beta - 2026-08-15

- 联机功能上线（不改变单机流程）：
  - 新增“服务器”页，可添加、编辑、删除常用服务器，保存地址与端口。
  - 一键延迟检测（TCP 连通性，4 秒超时），显示在线状态与延迟。
  - “启动并加入”会启动所选游戏配置并自动携带服务器参数直接进服，支持原版、Forge、Fabric、NeoForge 等全部加载器。
  - 新增外置登录（authlib-injector）：支持 LittleSkin、自建皮肤站等外置登录服务器；登录时自动下载并校验 authlib-injector 组件，凭据保存在 Windows 凭据管理器。
  - 启动时自动刷新外置登录令牌，失效时提示重新登录，不影响 Microsoft 与离线账户。

## SH启动器 0.5.15 Beta - 2026-08-15

- 修复更新检查总看到旧版本：之前更新清单优先走 jsDelivr CDN，CDN 缓存会让软件一直提示旧版本；现在优先检查 GitHub 发布页的最新清单（含国内加速镜像），发布后立即能检测到。
- 更新下载地址继续使用固定提交号的 CDN 链接，签名校验不变。

## SH启动器 0.5.14 Beta - 2026-08-15

- 游戏库的实例内存改为下拉选择：4 / 6 / 8 / 10 / 12 / 14 / 16 GB，或选“自定义”手动填写。

## SH启动器 0.5.13 Beta - 2026-08-15

- 游戏库页面可以直接修改已有实例的运行内存（2048–65536 MB），输入后回车或失焦即保存生效。

## SH启动器 0.5.12 Beta - 2026-08-15

- 将 kotlinforforge 识别为 Forge 语言加载器（其 JAR 本身没有 mods.toml），不再被误报为“缺失前置”，适用于所有整合包。

## SH启动器 0.5.11 Beta - 2026-08-15

- 修复窗口最小化、最大化/还原、关闭按钮无反应的问题（补全窗口控制权限）。
- 更新日志改为实时拉取仓库中的 changelog.json（走 jsDelivr CDN），每次发布新版本都会自动出现，不再依赖软件本体升级。

## SH启动器 0.5.10 Beta - 2026-08-15

- 更新清单直接提交到仓库并走 jsDelivr 国内 CDN 作为第一检查地址，解决“经常连不上更新服务器”的问题。
- 启动流程不再联网补齐前置：启动前只做本地快速校验，缺失时弹窗提供“自动补齐前置”和“仍要启动”两个按钮，启动不再被网络拖慢。

## SH启动器 0.5.9 Beta - 2026-08-15

- 启动检查跳过没有模组元数据的 jar（如 kotlinforforge 的 -all 库包），不再误报“无法识别加载器”而卡住启动。
- 模组声明 `${minecraft_version_range}` 时按“任意版本”处理，修复 Goety 等模组被误报不支持 1.20.1。
- 新增合成整合包自动化测试：自动生成 CurseForge / Modrinth / HMCL / MultiMC / MCBBS 五种格式的测试包，覆盖格式识别与依赖解析，防止兼容性回归。

## SH启动器 0.5.8 Beta - 2026-08-15

- 前置自动补齐超时放宽：单个依赖最多 30 秒、整轮最多 60 秒，避免网络稍慢就失败。

## SH启动器 0.5.7 Beta - 2026-08-15

- 修复 0.5.6 引入的启动参数顺序问题：实验性 GC 参数前缺少 `-XX:+UnlockExperimentalVMOptions`，导致 JVM 拒绝启动、Minecraft 退出码 1。

## SH启动器 0.5.6 Beta - 2026-08-15

- 启动前的前置自动补齐加入严格时间预算：每个依赖最多 10 秒、整轮最多 20 秒，超时立即停止并让用户先启动游戏，不再因为网络慢卡在启动前几分钟。
- 游戏启动参数加入 G1GC 等优化参数，Forge 大型整合包冷启动更快。

## SH启动器 0.5.5 Beta - 2026-08-15

- 下载客户端强制使用 HTTP/1.1，避免部分 CDN/代理下 HTTP/2 导致的“error decoding response body”和连接重置。
- 16MB 以上大文件自动 4 段并行下载并合并校验，显著提升模组/整合包下载速度。
- 下载并发数默认从 8 提高到 16。

## SH启动器 0.5.4 Beta - 2026-08-15

- CurseForge 依赖解析改为按真实 Mod ID 精确匹配：导入时从下载到的 JAR 里读取 modId 建立索引，不再依赖 modlist 顺序或文件名猜测，对所有 CurseForge 整合包通用。
- 依赖解析统一：优先 CurseForge 索引的 Mod ID 精确匹配，匹配不到再回退 Modrinth，避免“goety 被解析成 patchouli”这类错误。

## SH启动器 0.5.3 Beta - 2026-08-15

- 修复依赖解析误判：前置模组已存在（无论来自 CurseForge 还是 Modrinth）时按 Mod ID 去重，不再出现“已存在相同 Mod ID”这类误报（如 goety 依赖的 patchouli）。

## SH启动器 0.5.2 Beta - 2026-08-15

- 新增底部全局进度条：导入整合包、下载模组、安装游戏时固定显示在窗口底部，显示当前任务说明和实时百分比，不再让提示在页面里跳来跳去。
- 没有明确百分比的操作会显示流动动画，下载中的任务会显示合计进度。

## SH启动器 0.5.1 Beta - 2026-08-15

- 新增 HMCL 整合包导入：自动解析 modpack.json 的游戏版本与加载器，一键创建实例、解压 minecraft 覆盖文件并安装内置模组。
- 新增 MCBBS 整合包导入：识别 mcbbs.packmeta / manifest.json，解析版本与加载器，解压 overrides 覆盖文件。
- 主流整合包格式至此全部覆盖：CurseForge、Modrinth、HMCL、MultiMC、MCBBS。
- 以上格式导入后均自动接上基础游戏和加载器安装，一条龙完成。

## SH启动器 0.5.0 Beta - 2026-08-15

- 新增 MultiMC 整合包导入：自动解析 mmc-pack.json 的 Minecraft 版本和 Forge/NeoForge/Fabric/Quilt 加载器，一键创建实例并解压 .minecraft 覆盖文件。
- Modrinth / MultiMC 整合包导入后自动接着安装基础游戏和模组运行环境，一条龙完成，无需再手动点“检查并补齐游戏”和“安装模组环境”。
- 下载断流自动同源重试、镜像重试，CurseForge 前置按实例版本与加载器选择最优文件。

## SH启动器 0.4.11 Beta - 2026-08-15

- 下载连续无数据容忍时间从 60 秒放宽到 180 秒；单次失败后先用同一地址全新连接重试，再自动切换 BMCLAPI 镜像，大幅降低网络波动导致的失败率。
- CurseForge 前置模组解析改为按当前实例的游戏版本与加载器选择最优文件（不再只装索引里的固定文件），兼容性更高。

## SH启动器 0.4.10 Beta - 2026-08-15

- 前置模组未补齐时不再直接阻止启动：错误弹窗提供“仍要启动”和“前往模组页处理”两个入口，由用户决定。
- 修复发布流程推送安装包到仓库时与本地修改冲突的问题（0.4.9 构建失败原因）。

## SH启动器 0.4.9 Beta - 2026-08-15

- 云更新下载改为 jsDelivr 国内 CDN 分发：安装包同步托管在仓库 release-assets 目录，更新不再依赖不稳定的 GitHub 代理。
- 更新下载失败时自动重试最多 3 次，超时放宽到 5 分钟，失败率大幅下降。
- 发布流程自动把安装包推送到仓库并生成指向 CDN 的更新清单。

## SH启动器 0.4.8 Beta - 2026-08-15

- 前置自动补齐升级：Modrinth 找不到时，自动从 CurseForge 索引按名称匹配并下载安装（iceandfire、ftblibrary、tacz 等 CurseForge 专有模组都能识别）。
- 每个 CurseForge 整合包导入时会生成实例索引，并合并进全局索引；手动创建的实例也能使用已导入过的项目自动匹配。
- 模组页新增“从 CurseForge 网址安装”：粘贴项目/文件链接即可安装。
- 模组页把有问题的模组标红（加载器/游戏版本不兼容、缺少前置），并直接显示原因。
- 自动补齐版本回退限制在 Fabric/Quilt/Forge/NeoForge，不再误选 Spigot/Paper 等服务端插件版本。
- 启动检测到不兼容模组时弹窗询问是否删除（删除前先移入可恢复备份区）。
- 未知加载器类型的依赖（如 fzzy_config）允许自动补齐，不再中断。

## SH启动器 0.4.7 Beta - 2026-08-15

- 更新检查改为后台异步执行，不再等待网络导致启动动画卡住。
- 更新服务连接失败时，首页显示“暂时无法连接更新服务”提示和“重新检查”按钮，可一键重试。
- 云更新镜像更新为当前可用节点（gh-proxy.com、ghproxy.net 实测可用，ghfast.top 已失效移除）。

## SH启动器 0.4.6 Beta - 2026-08-15

- 开通 CurseForge 下载支持：导入 CurseForge 格式整合包时，自动并发补齐清单中的远程模组（官方公开接口 + HTTPS CDN，按文件大小校验），无需用户手动一个个下载。
- 与 Modrinth 一起覆盖市面上两大主流模组平台；下载器已用真实 CurseForge 文件（iceandfire 21.8MB）联网验证通过。
- 下载客户端升级为浏览器 UA 并对 CurseForge 自动附加来源头，避免被 CDN 拦截。

## SH启动器 0.4.5 Beta - 2026-08-15

- 整合包导入稳定性改进：内置模组不兼容时跳过并列出原因，不再中断整包导入，其余文件正常导入。
- 导入 CurseForge 格式整合包后，明确提示“N 个模组需要 CurseForge 下载，当前暂不支持”，避免误以为已完整安装。
- 使用真实整合包（落幕曲 1.6.5，1.20.1 Forge，474MB / 5846 文件）完成实测：格式与清单识别正确（266 个模组）、无路径穿越、检查耗时约 0.5 秒，符合流畅性要求。

## SH启动器 0.4.4 Beta - 2026-08-15

- 启动/安装失败改为弹窗提示；模组相关错误弹窗提供“前往模组页处理”入口，一键跳转删除问题模组。
- 自动补齐前置模组失败时，错误信息列出“需要该前置的模组”，明确是哪个模组引起的。
- 已安装模组新增搜索框（按名称/文件名/ID）与启用状态筛选。
- 在线模组搜索新增“模组环境”和“游戏版本”筛选，可覆盖当前游戏配置。
- 列表过滤全部使用 `useMemo` 缓存，避免大列表导致卡顿，保持轻量化。

## SH启动器 0.4.3 Beta - 2026-08-15

- 版本亮点从首页卡片改为弹窗：更新后首次启动自动弹出“本次版本亮点”，每个版本只弹一次，点“开始使用”关闭。
- 主页不再显示版本亮点卡片，界面更清爽；完整更新历史仍可通过侧边栏“更新日志”按钮查看。

## SH启动器 0.4.2 Beta - 2026-08-15

- 更新日志与版本亮点只展示已发布版本；未发布的版本（例如仍在审核中的 1.0.0）不再提前显示。

## SH启动器 0.4.1 Beta - 2026-08-15

- 启动前真检查：启动动画期间逐实例核对模组加载器、游戏版本和缺失前置；主页新增“启动前发现问题”卡片，明确指出哪个模组不兼容、原因和处理方法（删除或自动补齐），不再等到启动才报错。
- 自动补齐前置模组前先校验加载器：不兼容（例如只支持 Fabric 的模组装进 Forge 实例）不会再安装，错误信息会说明“哪个模组、为什么、怎么处理”。
- 模组版本匹配优先“同加载器任意版本”，再放宽到“同游戏版本任意加载器”，避免解析到错误加载器的版本。

## SH启动器 0.4.0 Beta - 2026-08-15

- 修复标题栏窗口无法拖动的问题：补全窗口拖拽所需权限，按住标题栏即可拖动窗口。
- 存档页新增“彻底删除”：双重确认后永久删除存档文件夹与数据库记录，不可恢复；普通“移除”仍保留可恢复备份。
- 设置页新增“清理缓存”：一键清理 SHA-1 下载缓存、Modrinth 缓存、Java 安装包缓存和残留的 `.part` 临时文件，并显示释放空间；不会删除任何游戏、模组、整合包或存档。

## SH启动器 0.3.0 Beta - 2026-08-15

- 下载源自动回退：版本清单、游戏文件、依赖库和资源包在官方源失败时自动改用 BMCLAPI 国内镜像，网络不稳也能正常下载；镜像文件仍按 SHA-1/大小校验，无法被篡改。
- 修复自动补齐前置模组时的循环依赖误报（如 cerbons_api）。
- 模组版本匹配逐级放宽（同版本同加载器 → 同版本 → 任意可用版本），expandability 等前置能自动解析。
- 启动动画改为白底清爽风格，与启动器整体配色一致。
- 侧边栏与启动页均标注当前版本号与“Beta 测试版”。
- 云更新地址增加国内镜像端点（gh-proxy、ghfast），更新检查更稳。

## SH启动器 0.2.0 Beta - 2026-08-15

- 下载模块新增下载详情窗口：点击任意下载任务即可查看项目、来源地址、已下载/总大小、实时速度、预计剩余时间、重试次数、校验值、创建/开始/更新时间与目标路径。
- 下载详情窗口随页面 2 秒自动刷新，进度与速度实时更新。
- 修复导出整合包/存档时保存对话框权限不足的问题。
- 默认只生成 NSIS 安装包，避免 MSI 打包中断。
- 发布流程改为自动生成并上传 latest.json，云更新开箱即用。

## SH启动器 0.1.4 - 2026-08-15

- 新增精美启动动画（基于 GitHub 3 万+ 星的 lottie-web 渲染）：动画期间并行完成游戏、游戏库、模组与整合包、Java 环境和启动器更新检查，并实时显示进度条与检查清单。
- 启动前自动识别缺失的前置模组，并从 Modrinth 自动下载补全，无需用户手动安装。
- 下载任务显示文件名、已下载大小、总大小、下载速度和预计剩余时间，并自动刷新。
- 修复自绘标题栏无法拖动窗口的问题。
- 首页新增新版本更新卡片；更新完成后自动弹出本次更新内容。

## SH启动器 0.1.2 - 2026-08-14

- 修复自绘标题栏“×”无法彻底退出启动器的问题。
- 导入与启动前检查模组的 Minecraft 版本范围、加载器和必需前置。
- 不兼容模组会用中文说明原因，不再直接进入 Forge 的红色报错页。
- 整合包覆盖备份移到专用目录，不再堆积在 `mods` 文件夹。
- 修复新版 Forge 安装器下载地址换行与生成型依赖导致的安装失败。

## SH启动器 0.1.0 - 2026-08-14

- 可以下载、检查并启动纯原版 Minecraft。
- 支持 Fabric、Quilt、Forge 和 NeoForge 模组运行环境。
- 可以在启动器里搜索 Modrinth 的模组和整合包，并自动筛选适合当前游戏版本的文件。
- 支持选择或拖入电脑里的模组、整合包、资源包、光影和存档。
- 可以安装游戏需要的 Java，并自动检查版本和是否为 64 位。
- 下载中断后可以继续，临时网络失败会自动重试，也可以手动取消。
- 每套游戏的版本、模组和存档分开保存，互不影响。
- 游戏数据固定放在 D 盘，不把游戏文件堆到 C 盘。
- 遇到启动失败时可以查看原因并导出诊断报告。
- 联机页面已经预留，当前按计划暂缓开放。
- SH启动器不包含 Minecraft 游戏本体；用户确认后才从 Mojang 官方网站下载。
# 0.1.3

- 接入免费的 Tauri + GitHub Releases 签名更新流程，更新失败时保留旧版本和用户数据。
- 安装器根据 Windows 语言显示简体中文或英文，中文系统默认显示中文。
- 设置页新增已有游戏目录选择，可优先复用 PCL、官方启动器或其他启动器的完整文件。
- 增加游戏无响应时的强制结束入口，并为启动器关闭按钮增加原生窗口关闭兜底。
- 将容易误解的“从 Mojang 下载”改成“检查并补齐游戏”，先检查本机和缓存后才联网下载缺失文件。
- 继续在启动前拦截游戏版本、加载器和模组依赖不兼容问题。
