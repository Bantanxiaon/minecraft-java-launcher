## SH启动器 v0.9.2 Beta（启动窗口兜底修复）

- 修复 0.9.1 仍可能“启动后没有窗口”的问题：主窗口的显示动作在某些环境下会失败，旧逻辑失败后仍会关闭启动小窗，导致程序无窗口可见。
- 现在只有确认主窗口真正可见后才关闭启动小窗；另外 Rust 侧新增看门狗，启动 1.5 秒后若主窗口仍未显示会自动重试并强制显示，绝不允许“无窗口”状态。
- 启动小窗不再被前端在失败路径下关闭。
